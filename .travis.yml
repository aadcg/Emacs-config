dist: xenial

language: minimal

sudo: required

before_install:
  - root add-apt-repository ppa:kelleyk/emacs && root apt install -y - qq emacs26
  - sudo mv ../.emacs.d ~
  - lsb_release -a
  - emacs -version

script:
  ./test_startup.sh
