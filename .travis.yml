services:
  - docker

sudo: required

before_install:
  - docker pull ubuntu:rolling
  # - sudo apt-get update && sudo apt-get install -y -qq snapd
  # - sudo snap install emacs
  # - sudo apt-get install -y -qq emacs
  - ls
  - pwd
  - sudo mv ../.emacs.d ~
  - ls ~
  - lsb_release -a

script:
  ./test_startup.sh
