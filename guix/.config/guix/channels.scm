;; (info "(guix) Channels")
;; (info "(guix) Creating a Channel")

;; (cons (channel
;;        (name 'variant-packages)
;;        (url "https://example.org/variant-packages.git"))
;;       %default-channels)
